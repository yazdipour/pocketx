<UserControl
    x:Class="PocketX.Controls.TagsListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PocketX.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uwpChipsX="using:UWPChipsX"
    mc:Ignorable="d">
    <GridView ItemsSource="{x:Bind Tags, Mode=OneWay}" 
              IsItemClickEnabled="True" 
              SelectionMode="None"
              ItemClick="ItemClick">
        <GridView.ItemTemplate>
            <DataTemplate>
                <!-- ReSharper disable once Xaml.InvalidResourceType -->
                <Border Background="{ThemeResource AccentColor}" Padding="12 4"
                        CornerRadius="8" RequestedTheme="Dark" Margin="4">
                    <TextBlock Text="{Binding}"/>
                </Border>
            </DataTemplate>
        </GridView.ItemTemplate>
    </GridView>
</UserControl>
